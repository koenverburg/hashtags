import { {{ properCase name }}Store } from '../{{ properCase name }}.store'

describe('{{ properCase name }}Store', () => {
  it('fetches {{ lowerCase name }}s', () => {
    const store = new {{ properCase name }}Store()
    expect(store.fetch).toBeDefined()
  })
})
