/* tslint:disable no-implicit-dependencies */
import React from 'react'
import { shallow } from 'enzyme'
import { Provider } from 'mobx-react'
import { {{ properCase name }} } from '..'
import { {{ properCase name }}Store } from '../{{ properCase name }}.store'

describe('feature - <{{ properCase name }} />', () => {
  let store: {{ properCase name }}Store

  beforeAll(() => {
    store = new {{ properCase name }}Store()
  })

  it('renders', () => {
    const feature = shallow(
      (
        <Provider store={store}>
          <{{ properCase name }} />
        </Provider>
      ),
    )

    expect(feature).toMatchSnapshot()
  })
})
