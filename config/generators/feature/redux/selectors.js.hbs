import { createSelector } from 'reselect'

export const dataSelector = state => state.{{ lowerCase name }}.data
export const errorSelector = state => state.{{ lowerCase name }}.error
export const statusSelector = state => state.{{ lowerCase name }}.status
